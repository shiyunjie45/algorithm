package com.sky.algorithmleetcode;

/*
给你一个正整数的数组 A（其中的元素不一定完全不同），请你返回可在 一次交换（交换两数字 A[i] 和 A[j] 的位置）后得到的、按字典序排列小于 A 的最大
可能排列。 如果无法这么操作，就请返回原数组。   示例 1： 输入：[3,2,1] 输出：[3,1,2] 解释： 交换 2 和 1    示例 2： 输入：[
1,1,5] 输出：[1,1,5] 解释： 这已经是最小排列    示例 3： 输入：[1,9,4,6,7] 输出：[1,7,4,6,9] 解释： 交换 9 和
 7    示例 4： 输入：[3,1,1,3] 输出：[1,3,1,3] 解释： 交换 1 和 3    提示：  	1 	1
*/

题目描述

给你一个正整数的数组 A（其中的元素不一定完全不同），请你返回可在 一次交换（交换两数字 A[i] 和 A[j] 的位置）后得到的、按字典序排列小于 A 的最大可能排列。 如果无法这么操作，就请返回原数组。

示例 1：

输入：[3,2,1]
输出：[3,1,2]
解释： 交换 2 和 1

示例 2：

输入：[1,1,5]
输出：[1,1,5]
解释： 这已经是最小排列

示例 3：

输入：[1,9,4,6,7]
输出：[1,7,4,6,9]
解释： 交换 9 和 7

示例 4：

输入：[3,1,1,3]
输出：[1,3,1,3]
解释： 交换 1 和 3

提示：

1 <= A.length <= 10000
1 <= A[i] <= 10000

Java代码实现

class L1053Solution {
    public int[] prevPermOpt1(int[] A) {
        if (A == null || A.length == 0) { //判断输入是否合法
            return new int[0];
        }
        int i = A.length - 2; //从倒数第二个数开始扫描
        while (i >= 0 && A[i] <= A[i + 1]) { //找到第一个违反降序的位置
            i--;
        }
        if (i >= 0) { //存在这样的位置
            int j = A.length - 1; //从后往前扫描，找到第一个比A[i]小的数
            while (j >= 0 && A[j] >= A[i]) {
                j--;
            }
            swap(A, i, j); //交换A[i]和A[j]
        }
        return A;
    }

    private void swap(int[] A, int i, int j) {
        int tmp = A[i];
        A[i] = A[j];
        A[j] = tmp;
    }
} 